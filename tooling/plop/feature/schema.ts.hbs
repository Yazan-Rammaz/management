import { z } from "zod";

export const {{camelCase name}}Schema = z.object({
  id: z.string(),
  // TODO: model the {{kebabCase name}} fields to match the NestJS DTO.
  name: z.string(),
});

export type {{pascalCase name}} = z.infer<typeof {{camelCase name}}Schema>;
